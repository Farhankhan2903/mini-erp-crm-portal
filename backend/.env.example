# Application Port
PORT=5001

# PostgreSQL Database Connection String (Neon, Render, or Docker Postgres)
DATABASE_URL="postgresql://postgres:password@localhost:5432/mini_erp_crm?schema=public"

# JWT Token Secret Key
JWT_SECRET="super-secret-jwt-key-change-in-production"

# Environment Mode (development / production)
NODE_ENV="development"
